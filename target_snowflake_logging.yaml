version: 1
disable_existing_loggers: false
handlers:
  snowflake:
    class: logging.FileHandler
    filename: snowflake.log
loggers:
  snowflake.connector:
    level: DEBUG
    handlers: [ snowflake ]
